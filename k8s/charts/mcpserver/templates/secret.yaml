{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
stringData:
  postgres-connection-string: {{ .Values.secrets.postgresConnectionString | quote }}
{{- end }}
